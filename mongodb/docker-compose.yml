version: '3.3'

services:
  mongodb:
    image: mongo:latest
    container_name: local_mongodb
#    volumes:
#      - /Users/hope/my_mongo_docker2:/data/db
    ports:
      - "27017:27017"
    restart: always
